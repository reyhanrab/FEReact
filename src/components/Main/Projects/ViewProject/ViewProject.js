import { Box } from "@mui/material";
import * as React from "react";

export default function ViewProject() {
  return <Box sx={{ height: "170px", display: "flex", justifyContent: "center", alignItems: "center" }}>
    No projects found
    </Box>;
}
